// Verion
env.version = '1.0v'

// Global params
params.CPUs = '20'
process.debug = true

// Docker config
docker.runOptions = '-u $(id -u):$(id -g)'
process.container = 'mqtl-image'
docker.enabled = true
